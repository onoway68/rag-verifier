name: RAG Verifier Unit Gate

on:
  push:
  pull_request:

jobs:
  verifier-unit-tests:
    name: Deterministic verifier tests
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install deterministic test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-ci.txt

      - name: Prove ML runtime is not imported
        run: |
          python - <<'PY'
          import sys

          from verifier import RAGVerifier
          from nli_provider import FakeNLIProvider
          from embedding_provider import FakeEmbeddingProvider
          from retriever import Retriever

          verifier = RAGVerifier(
              chunks={"x": "Evidence."},
              retrieved_ids={"x"},
              nli_provider=FakeNLIProvider()
          )

          retriever = Retriever(
              chunks={"x": "Evidence."},
              embedding_provider=FakeEmbeddingProvider(
                  {
                      "question": [1.0, 0.0],
                      "Evidence.": [1.0, 0.0]
                  }
              )
          )

          retriever.retrieve("question")

          assert "torch" not in sys.modules
          assert "transformers" not in sys.modules
          assert "sentence_transformers" not in sys.modules

          print(
              "Deterministic verifier and retriever "
              "are ML-runtime independent."
          )
          PY

      - name: Run deterministic RAG gate
        run: |
          python -m pytest -m "not integration" -v